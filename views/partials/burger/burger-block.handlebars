<li>
	{{burger_name}}

	<button class="change-devoured btn btn-sm btn-outline-secondary" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour it!{{else}}Devoured{{/if}}
	</button>
</li>